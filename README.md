Сделад консольную программу которая решает задачу с разноцветными жидкостями в пробирках как тестовое задание на позицию Младший Fullstack-разработчик.  
С Java знаком очень мало и изучал всё практически с нуля.  
Писал сервер с 09.10.2025 по 10.10.2025 и с 14.10.2025 по 15.10.2025.  
Результат считаю неудволетворительным - так как первый вариант был простым обходом в ширину, но он падал с ошибкой выхода за пределы памяти на примере из тз.  
Пробовал добавлять эвристику, но она отсекал нужные пути или вычисляла пример из тз больше двух часов дойдя до глубины в 13 ходов.  
  
Данный код (поиск с возвратом) находит решение, но через много повторяющихся ходов, зато быстро.   
К сожалению сильно закопался в решении и неуспел до конца разобраться.  
  
Запуск:  
./gradlew run --args="1 input.txt"  запускает программу с примером из файла input.txt  
./gradlew run --args="1"            запускает программу с примером из файла и просит указать имя файла для считывания  
./gradlew run --args="2"            ручной ввод (с gradlew не работает)  
./gradlew run --args="3"            проход встроеных простых тестов  
./gradlew run --args="input.txt"    запускает пример из указанного файла  
или  
mkdir -p out  
javac -d out $(find src/main/java -name "*.java")  
java -cp out tubes.Main  
